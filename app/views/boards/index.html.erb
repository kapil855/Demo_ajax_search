 <div class="container mt-4">
  <div class="row">
    <div class="col-md-4">
      <ul class="card bg-secondary">
        <% @boards.each do |board| %>
          <li><%= board.name %></li>
        <% end %>
      </ul>
    </div>
    <div class="col-md-4">
      <ul class="card bg-secondary">
        <% @boards.each do |board| %>
          <li><%= board.name %></li>
        <% end %>
      </ul>
    </div>
    <div class="col-md-4">
      <ul class="card bg-secondary">
        <% @boards.each do |board| %>
          <li><%= board.name %></li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
